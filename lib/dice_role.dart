import 'package:flutter/material.dart';

class DiceRole extends StatelessWidget {
  const DiceRole({super.key});

  @override
  Widget build(context) {
    return Image.asset(
      'assets/images/dice-1.png',
      height: 200,
    );
  }
}
